'use strict';

/**
 * maisadour-podsolnechnik router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::maisadour-podsolnechnik.maisadour-podsolnechnik');
